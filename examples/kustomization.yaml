apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  - sts-scheduler.yaml
configMapGenerator:
  - name: sts-scheduler-config
    files:
      - config.yaml=scheduler-config.yaml
